parameters:
  twig.config:
    debug: true
    auto_reload: true
    cache: false
services:
  assignment.timezoneService:
    class: Drupal\assignment\TimeZoneService
    arguments: ['@config.factory']